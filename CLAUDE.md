# 开发代理工作规范

## 基本原则

每次修改代码后，**必须**按以下步骤验证：

1. **编写测试脚本** - 验证核心逻辑（使用独立测试数据库）
2. **自己启动服务** - 确认服务正常启动
3. **自己运行测试** - 执行自动化测试验证
4. **不污染现有数据** - 测试使用独立数据
5. **不推诿给用户测试** - 所有测试我自己完成

## 修改后的检查清单

- [ ] 代码语法正确
- [ ] 编写或更新测试用例
- [ ] **自己**启动服务确认端口可访问
- [ ] **自己**运行自动化测试
- [ ] 界面操作验证功能（如需）
- [ ] 清理测试数据

## 测试数据规范

- 测试数据库使用 `data/test_*.db`
- 测试商品名称标记为 `测试商品-*`
- 测试完成后可清理

## 测试工具

- `test_web_integration.py` - HTTP 集成测试，模拟用户完整操作流程
- 测试流程：新增商品 → 添加保价 → 提交客服 → 确认到账 → 撤销

## 教训记录

- **2026-05-20** - 修改后没有自己测试，让用户发现 404 错误，非常不专业。**必须自己完成所有测试步骤。**

- **2026-05-20** - 项目有多个启动脚本（start_simple.py、start_fixed.py、run_web_simple.py），修改功能时只更新了部分文件，但用户运行的是另一个没更新的脚本，导致筛选功能无效。**教训：**
  1. 先确认用户实际在运行哪个脚本
  2. 必须保证所有相关文件都同步更新
  3. 修改后要实际启动并测试用户正在用的那个脚本

- **2026-05-26** - API key 硬编码在 `tests/list_products.py` 和 `tests/show_history.py` 中，被提交到 GitHub 导致安全事件，仓库被迫删除重建。**教训：**
  1. 所有敏感信息（API key、token、密码）必须通过环境变量传入
  2. 提交前必须检查是否有硬编码的敏感信息
  3. 文件内容修改后也要检查是否引入了新的敏感信息

## 启动脚本说明

- `app.py` - Web UI 入口（FastAPI，用户实际在用，监听 0.0.0.0:9000）
- `monitor_all_products.py` - 后台价格监控脚本（每10分钟检查一次）
- `start_monitor.sh` - 启动后台监控（nohup 方式）
- `stop_monitor.sh` - 停止后台监控
- `run_monitor_once.py` - 手动运行一次价格检查

## 项目架构说明

- **Web UI**: `app.py` - FastAPI + HTML 模板
- **价格查询**: `src/skills/smzdm_opencli.py` - 使用 opencli adapter 浏览器自动化查询 smzdm 好价
- **后台监控**: `monitor_all_products.py` - APScheduler 定时任务
- **数据存储**: SQLite - `data/price_monitor.db`
- **通知**: 企业微信 Webhook

## 监控逻辑

1. 每10分钟检查所有商品
2. 查询 smzdm 好价，只保留最近2天内的价格
3. 比较基准 = min(商品原价, 待保价, 已保价)
4. 如果 smzdm 价格 < 基准价，发送企业微信通知
5. 已发送通知的链接记录在 `log/sent_urls.txt`，避免重复发送

## 需要同步更新的文件

修改功能时，注意以下文件可能需要同步更新：

- `monitor_all_products.py` - 监控脚本
- `src/skills/smzdm_opencli.py` - smzdm 查询技能
- `src/storage/crud.py` - 数据库 CRUD
